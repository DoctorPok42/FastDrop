services:
  frontend:
    image: doctorpok/fastdrop:frontend-latest
    restart: unless-stopped
    container_name: fastdrop-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    image: doctorpok/fastdrop:backend-latest
    restart: unless-stopped
    container_name: fastdrop-backend
    ports:
      - "8000:8000"
